import { PipeCategoriaPipe } from './pipe-categoria.pipe';

describe('PipeCategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeCategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
